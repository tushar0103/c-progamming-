#include<stdio.h>
#include<conio.h>
#include<Math.h>
void main(){
    int n;
    printf("enter the nu mber\n");
    scanf("%d",&n);
   if(prime(n)){
    printf("prme");
   }else{
    printf("not");
   }
}
int prime(int n){
    for(int i=2; i<=sqrt(n); i++){
        if(n%i==0){
            return 0;
        }
    }
    return n;
}
